# LabManager 前端与后端接口对比表

生成时间：2026-05-06

## 概述

- **前端基础 URL**: `/api/ai` (配置在 `VITE_AI_API_BASE_URL`)
- **Python 后端端口**: 8001
- **TS 后端端口**: 8000

---

## 接口对比详情

### ✅ 已实现且匹配的接口

| 接口路径 | 方法 | 前端调用位置 | Python 后端实现 | 状态 |
|---------|------|------------|----------------|------|
| `/health` | GET | `probeHttpAiGateway()` | `ai_compat.py:270` | ✅ 已实现 |
| `/auth/login` | POST | `loginWithBootstrapUser()` | `ai_compat.py:307` | ✅ 已实现 |
| `/auth/me` | GET | - | `ai_compat.py:324` | ✅ 已实现 |
| `/settings` | GET | `getSettings()` | `ai_compat.py:336` | ⚠️ 需要数据库 |
| `/settings` | PATCH | `updateSettings()` | `ai_compat.py:342` | ⚠️ 需要数据库 |
| `/chemicals` | GET | `listChemicals()` | `ai_compat.py:356` | ✅ 已实现 |
| `/equipment` | GET | `listEquipment()` | `ai_compat.py:362` | ✅ 已实现 |
| `/import-batches` | GET | `listImportBatches()` | `ai_compat.py:368` | ✅ 已实现 |
| `/imports/chemicals` | POST | `importChemicals()` | `ai_compat.py:375` | ✅ 已实现 |
| `/imports/equipment` | POST | `importEquipment()` | `ai_compat.py:405` | ✅ 已实现 |
| `/rules/inspect` | POST | `inspectRuleEvents()` | `ai_compat.py:452` | ✅ 已实现 |
| `/rules/execute` | POST | `executeRuleEvent()` | `ai_compat.py:544` | ✅ 已实现 |
| `/tasks` | GET | `listTasks()` | `ai_compat.py:627` | ✅ 已实现 |
| `/tasks/{task_id}` | GET | `getTaskActions()` | `ai_compat.py:651` | ✅ 已实现 |
| `/tasks/{task_id}/status` | PATCH | `updateTaskStatus()` | `ai_compat.py:673` | ✅ 已实现 |
| `/tasks/{task_id}/assignee` | PATCH | `assignTask()` | `ai_compat.py:778` | ✅ 已实现 |
| `/approvals` | GET | `listApprovals()` | `ai_compat.py:813` | ✅ 已实现 |
| `/approvals` | POST | `createApprovalForTask()` | `ai_compat.py:823` | ✅ 已实现 |
| `/approvals/{approval_id}/process` | PATCH | `processApproval()` | `ai_compat.py:879` | ✅ 已实现 |
| `/reports` | GET | `listReports()` | `ai_compat.py:928` | ✅ 已实现 |
| `/reports/generate` | POST | `generateReport()` | `ai_compat.py:934` | ✅ 已实现 |
| `/reports/{report_id}` | DELETE | `deleteReport()` | `ai_compat.py:953` | ✅ 已实现 |
| `/reports/{report_id}/pdf` | GET | `exportReportPdf()` | `ai_compat.py:963` | ✅ 已实现 |
| `/report-delivery/mappings` | GET | `listReportDeliveryMappings()` | `ai_compat.py:983` | ✅ 已实现 |
| `/report-delivery/mappings` | POST | `saveReportDeliveryMapping()` | `ai_compat.py:989` | ✅ 已实现 |
| `/report-delivery/mappings/{mapping_id}` | PATCH | `saveReportDeliveryMapping()` | `ai_compat.py:998` | ✅ 已实现 |
| `/report-delivery/configs` | GET | `listReportDeliveryConfigs()` | `ai_compat.py:1008` | ✅ 已实现 |
| `/report-delivery/configs` | POST | `saveReportDeliveryConfig()` | `ai_compat.py:1014` | ✅ 已实现 |
| `/report-delivery/configs/{config_id}` | PATCH | `saveReportDeliveryConfig()` | `ai_compat.py:1023` | ✅ 已实现 |
| `/report-delivery/records` | GET | `listReportDeliveryRecords()` | `ai_compat.py:1033` | ✅ 已实现 |
| `/report-delivery/send` | POST | `sendReport()` | `ai_compat.py:1039` | ✅ 已实现 |
| `/agents/task-tracking/execute` | POST | - | `ai_compat.py:1060` | ✅ 已实现 |
| `/agents/reporting/execute` | POST | - | `ai_compat.py:1105` | ✅ 已实现 |
| `/routing/validate` | GET | - | `ai_compat.py:1067` | ✅ 已实现 |

---

## Python 后端额外的原生接口（非 /api/ai 前缀）

这些是 Python 后端的原生实现，不在前端兼容层中：

### 健康检查
- `GET /health/live` - 存活检查
- `GET /health/ready` - 就绪检查

### 导入（原生）
- `POST /api/imports/manual` - 手动导入
- `POST /api/imports/batch` - 批量导入
- `GET /api/imports/batches` - 获取批次列表
- `GET /api/imports/batches/{batch_id}` - 获取批次详情

### 规则处理
- `POST /api/rules/process` - 规则处理

### 邮件
- `POST /api/email/send` - 发送邮件
- `GET /api/email/status/{task_id}` - 邮件状态
- `POST /api/email/history` - 邮件历史
- `POST /api/email/retry/{record_id}` - 重试邮件

### PDF 导出
- `POST /api/pdf/export` - PDF 导出

### 报告（原生）
- `POST /api/reports/daily` - 生成日报
- `POST /api/reports/weekly` - 生成周报
- `GET /api/reports/tasks/{task_id}` - 获取报告任务状态

### 任务（原生）
- `GET /api/tasks` - 任务列表
- `GET /api/tasks/{task_id}` - 任务详情
- `POST /api/tasks` - 创建任务
- `PATCH /api/tasks/{task_id}/status` - 更新任务状态
- `PATCH /api/tasks/{task_id}/assignee` - 分配任务

### 设置（原生）
- `GET /api/settings` - 获取设置
- `PATCH /api/settings` - 更新设置
- `GET /api/settings/history` - 设置历史

### 库存（原生）
- `GET /api/inventory/chemicals` - 化学品列表
- `GET /api/inventory/equipment` - 设备列表

### 导入批次
- `GET /api/import-batches` - 导入批次列表

### 活动日志
- `GET /api/activity-logs/tasks/{task_id}` - 任务活动日志
- `GET /api/activity-logs/approvals/{approval_id}` - 审批活动日志
- `GET /api/activity-logs/actors/{actor_id}` - 操作者活动日志
- `GET /api/activity-logs` - 活动日志列表

### Gateway 导入
- `POST /api/gateway/imports/batch` - Gateway 批量导入
- `GET /api/gateway/imports/batches` - Gateway 批次列表

---

## 当前问题分析

### 1. 数据库配置缺失

**错误**: `RuntimeError: DATABASE_URL not configured`

**影响接口**:
- `/settings` (GET/PATCH) - 需要从数据库读取配置

**解决方案**: 在 `python_backend/.env` 中配置 `DATABASE_URL`

```env
DATABASE_URL=sqlite:///./labmanager.db
# 或使用 PostgreSQL
# DATABASE_URL=postgresql://user:password@localhost/dbname
```

### 2. 所有接口实际上都已实现

从代码分析来看，**所有前端需要的接口在 Python 后端中都已经实现**！

之前看到的 404 错误可能是因为：
1. 后端启动时有错误（DATABASE_URL 未配置）
2. 路由注册有问题
3. 前端请求的 URL 路径不正确

### 3. 接口实现方式

Python 后端使用了**兼容层模式**：
- 所有接口都在 `app/api/ai_compat.py` 中实现
- 使用 `@router` 装饰器，前缀为 `/api/ai`
- 大部分接口使用内存数据（mock data）
- 部分接口可以切换到真实的 Python 服务（通过环境变量控制）

---

## 迁移状态

根据 CLAUDE.md 的迁移守则：

### M-04: 分阶段切流守则

**第一阶段**（应该先切换）:
- ✅ rules - 已实现
- ✅ import - 已实现
- ✅ reports - 已实现
- ✅ PDF - 已实现
- ✅ email - 已实现
- ✅ async tasks - 已实现

**第二阶段**（然后切换）:
- ✅ task API - 已实现
- ✅ approval API - 已实现
- ✅ activity log main API - 已实现

---

## 建议

1. **立即配置 DATABASE_URL** - 这是当前唯一的阻塞问题
2. **验证路由注册** - 确认 `ai_compat_router` 正确注册到主应用
3. **测试接口** - 配置数据库后，逐个测试前端调用的接口
4. **监控日志** - 观察后端日志，确认请求是否到达正确的处理器

---

## 总结

✅ **好消息**: Python 后端已经实现了前端需要的所有接口！

⚠️ **当前问题**: 只需要配置 `DATABASE_URL` 环境变量

🎯 **下一步**: 配置数据库连接，重启后端，前端应该就能正常工作了
