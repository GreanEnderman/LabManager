# 🚀 快速启动自动化流程

## ✅ 已完成的配置

我已经为你创建了以下文件：

1. ✅ `app/tasks/beat_schedule.py` - Celery Beat 定时任务配置
2. ✅ `app/rules/tasks.py` - 规则引擎扫描任务
3. ✅ `app/reports/scheduled_tasks.py` - 报告生成任务
4. ✅ `app/tasks/celery_app.py` - 更新了 Celery 配置
5. ✅ `test_celery_setup.py` - 配置测试脚本

## ⏱️ 测试友好的时间间隔

为了方便你快速测试，我设置了很短的时间间隔：

| 任务 | 测试间隔 | 生产建议 |
|------|---------|---------|
| SLA 监控 | **1 分钟** | 5 分钟 |
| 规则扫描 | **2 分钟** | 10 分钟 |
| 日报生成 | **5 分钟** | 每天 8:00 |
| 周报生成 | **10 分钟** | 每周一 9:00 |

这样你可以在几分钟内看到效果！

---

## 🎯 现在开始启动

### 第 1 步：启动 Redis

```bash
# 使用 Docker（推荐）
docker run -d --name labmanager-redis -p 6379:6379 redis:alpine

# 验证 Redis 是否运行
docker ps | grep labmanager-redis
```

### 第 2 步：更新 .env 配置

编辑 `python_backend/.env`，取消注释以下行：

```bash
# Redis 配置
LABMANAGER_PY_REDIS_URL=redis://localhost:6379/0

# Celery 配置
LABMANAGER_PY_CELERY_BROKER_URL=redis://localhost:6379/1
LABMANAGER_PY_CELERY_RESULT_BACKEND=redis://localhost:6379/2
```

### 第 3 步：测试配置

```bash
cd python_backend
.venv\Scripts\activate
python test_celery_setup.py
```

应该看到：
```
✅ Celery app 加载成功
✅ Beat schedule 已加载
✅ Redis 连接成功
✅ 数据库连接成功
🎉 所有测试通过！
```

### 第 4 步：启动服务（4 个终端）

**终端 1：Python 后端**
```bash
cd python_backend
.venv\Scripts\activate
uvicorn app.main:app --reload --port 8001
```

**终端 2：Celery Worker**
```bash
cd python_backend
.venv\Scripts\activate
celery -A app.tasks.celery_app worker --loglevel=info --pool=solo
```

**终端 3：Celery Beat**
```bash
cd python_backend
.venv\Scripts\activate
celery -A app.tasks.celery_app beat --loglevel=info
```

**终端 4：前端**
```bash
cd frontend
npm run dev
```

---

## 🔍 验证自动化是否工作

### 1. 查看 Celery Worker 日志

应该看到任务被注册：
```
[tasks]
  . reports.generate_daily
  . reports.generate_weekly
  . rules.scan_and_execute
  . sla.run_inspection
```

### 2. 查看 Celery Beat 日志

应该看到定时任务被调度：
```
Scheduler: Sending due task sla-inspection (sla.run_inspection)
Scheduler: Sending due task rules-scan (rules.scan_and_execute)
```

### 3. 等待 1-2 分钟

- **1 分钟后**：SLA 监控任务应该运行
- **2 分钟后**：规则扫描任务应该运行
- **5 分钟后**：日报生成任务应该运行

### 4. 查看数据库

```sql
-- 查看自动创建的任务
SELECT id, type, title, status, assignee_name, created_at 
FROM tasks 
ORDER BY created_at DESC 
LIMIT 10;

-- 查看审计日志
SELECT id, action_type, detail, actor_name, created_at 
FROM task_actions 
ORDER BY created_at DESC 
LIMIT 10;
```

---

## 🧪 手动触发测试

如果不想等待，可以手动触发任务：

```bash
# 手动触发规则扫描
celery -A app.tasks.celery_app call rules.scan_and_execute

# 手动触发 SLA 检查
celery -A app.tasks.celery_app call sla.run_inspection

# 手动触发日报生成
celery -A app.tasks.celery_app call reports.generate_daily
```

---

## 📊 监控命令

```bash
# 查看活跃任务
celery -A app.tasks.celery_app inspect active

# 查看已注册任务
celery -A app.tasks.celery_app inspect registered

# 查看定时任务
celery -A app.tasks.celery_app inspect scheduled

# 查看 Worker 状态
celery -A app.tasks.celery_app inspect stats
```

---

## 🎯 测试场景

### 场景 1：低库存自动处理

1. 在数据库中插入一个低库存化学品：
```sql
INSERT INTO chemicals (id, name, current_quantity, threshold, status)
VALUES ('test-chem-001', '测试化学品', 5, 10, 'normal');
```

2. 等待 2 分钟（规则扫描）

3. 查看是否自动创建了任务：
```sql
SELECT * FROM tasks WHERE source_id = 'test-chem-001';
```

### 场景 2：设备故障自动处理

1. 在数据库中插入一个故障设备：
```sql
INSERT INTO equipment (id, name, status)
VALUES ('test-equip-001', '测试设备', 'fault');
```

2. 等待 2 分钟

3. 查看是否自动创建了任务

---

## 🐛 常见问题

### Q: Celery Worker 启动失败
```bash
# 检查 Redis 是否运行
redis-cli ping

# 检查 .env 配置
cat .env | grep CELERY
```

### Q: 定时任务不执行
```bash
# 确保 Celery Beat 正在运行
# 查看 Beat 日志是否有调度记录
# 查看 Worker 日志是否收到任务
```

### Q: 任务执行失败
```bash
# 查看 Worker 日志中的错误堆栈
# 检查数据库连接
# 检查 LLM API 配置
```

---

## 🎉 成功标志

当你看到以下情况，说明自动化已经正常运行：

1. ✅ Celery Worker 显示 "ready"
2. ✅ Celery Beat 定期发送任务
3. ✅ Worker 日志显示任务执行成功
4. ✅ 数据库中出现自动创建的任务
5. ✅ 前端可以看到自动创建的任务

---

## 📝 下一步

测试通过后，你可以：

1. 调整 `beat_schedule.py` 中的时间间隔到生产环境的值
2. 添加更多的定时任务
3. 配置邮件通知
4. 添加监控和告警

**祝测试顺利！** 🚀
