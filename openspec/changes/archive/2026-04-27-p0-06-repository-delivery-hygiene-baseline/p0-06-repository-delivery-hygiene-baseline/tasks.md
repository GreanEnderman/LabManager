## 1. 现状盘点与边界确认

- [x] 1.1 盘点当前仓库中已出现的压缩包、导出图片、构建输出、依赖目录和环境文件类型，确认需要被忽略或保留的对象
- [x] 1.2 识别仓库内需要继续纳入版本控制的源码资产、模板文件和文档文件，避免忽略规则误伤业务必需内容

## 2. 忽略规则与模板边界落地

- [x] 2.1 在仓库根目录新增或收敛 `.gitignore`，覆盖 `node_modules`、`dist`、`build`、缓存、日志、压缩包和导出型图片产物
- [x] 2.2 建立环境文件边界，忽略真实 `.env` 及本地变体，并保留或补充不含敏感值的模板类配置文件策略
- [x] 2.3 如存在误入仓的派生产物或交付压缩物，按基线调整仓库交付面，只保留可审查的源文件与模板

## 3. 交付文档与验证

- [x] 3.1 在 README 或 docs 中补充仓库提交策略与交付边界说明，明确哪些文件允许提交、哪些只能通过发布或外部交付
- [x] 3.2 复查 Git 状态与目录结果，确认仓库交付面已收敛到“可审查、可构建、可发布”的最小集合
